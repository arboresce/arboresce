# Lifecycle contract checkpoint evidence

Date: 2026-09-08. The starting source revision is
`90c640cc36ae67970a172e01ff25d12ee55be884`, which records the completed context
contract checkpoint. The lifecycle owner defines eight operation kinds,
complete bounded execution records, cancellation, upload controls and explicit
analysis, revoke/hold/purge/propagation, financial delivery, attributed feedback,
retained recognition/retirement and six independent continuity families.
Service implementation and qualification remain assigned to their later owners.

## Independently authored inputs and reconciliation

The original schema and literal fixture authors worked independently. Before
comparison the fixture author froze 7,068 cases across 196 named targets:
166 public and 30 internal definitions. The target namespaces resolve to their
exact owning libraries. Public definitions may not import internal definitions.
Neither application library admits an instance at its root.

The first qualification attempt passed nine commands and timed out during the
complete lifecycle check after 900.014 seconds. That command was terminated with
exit -9. It was a failed attempt; no negative fixture or qualification success
was inferred. The original inputs, both streams and command ledger remain
retained separately.

A separate complete diagnostic against those same original bytes ran for
1,383.771 seconds and reported 221 errors across 144 distinct case identities.
Its observer finished successfully but did not propagate the validation
library's returned exit code. That observer result is not a validation pass.
Independent review mapped every reported case to its governing requirement and
preserved all original bytes and observations.

The schema repair adds required committed-effect kinds for the seven successful
operation branches. It constrains hold-release event/response purge states,
nonpurged material availability and control revision, and restricts the first
revoke response to its legal initial purge state. It does not remove legitimate
late effects from failed or cancelled operations. The internal continuity
library is unchanged from its original freeze.

The first independent fixture repair corrected 83 structural positive values
and five cancellation examples. One existing negative expectation changed to
positive because its unchanged policy-reference value was already a legitimate
alternate under the existing context contract. It added 230 cases while retaining
all original identities, order and targets. Review found remaining incorrect
running/cancelled seeds and masked negative baselines; that failed review remains
part of the reconciliation history.

The second repair corrected 61 running positive controls, 47 cancelled positive
records and four cancellation-clock examples. It repaired unrelated seed defects
in 767 negative cases while preserving each deliberate invalid mutation. It
added 108 exact old positive values as independently specified negative controls.
No expectation changed from the first repair, and all previous case identities,
order and targets remain. Independent review audited every changed value and
mapped all 144 original diagnostic identities without omissions.

Before the URI-format repair below, the resulting lifecycle family contained
7,406 cases: 1,111 positive and 6,295 negative across the same 196 targets, with
6,837 public and 569 internal cases. Every target has both accepted and rejected
examples. Earlier invalid copies and malformed-operation probes remain present.

Two unchanged negative oracles also exposed an existing shared problem-schema
defect. The common owner already forbids a direct `problem` extension, including
null and false. The shared schema omitted that exact property prohibition.
Adding `problem: false` repairs the existing rule for every importing consumer;
it does not close allowed additive extensions or forbid legitimate outer typed
problem fields. Three independently authored common regressions cover null,
false and a complete nested problem object. All earlier 248 common cases and
expectation entries remain unchanged. The common family now has 251 cases:
66 accepted and 185 rejected. The other five existing application libraries retain their previous bytes.
The later URI append changes acquisition and lifecycle fixture inputs only.

## Required URI-format repair and actual direct observations

A separate diagnostic established that the primary tool did not register URI
or URI-reference checking: malformed percent escapes and bracketed hosts were
accepted despite the existing acquisition URI requirement. Date/date-time
negative controls rejected correctly. The URI checker is optional upstream;
leaving format checking enabled did not establish that its implementation was
installed. No application schema semantics changed to make these values pass.

The direct pin `rfc3986-validator==0.1.1` selects jsonschema's existing upstream
fallback. All fourteen earlier external lock package tables remain unchanged;
the lock adds this one package and the virtual project's dependency metadata.
The exact 4,242-byte wheel has SHA-256
`2f235c432ef459970b4306369336b9d5dbdda31b510ca1e327636e01f528bfa9`,
MIT licensing and no additional dependency. The inspected module imports only
Python re. This package is a validator-tool dependency, not product runtime.

An independently authored and reviewed corpus adds sixteen URI variants to
each of three real targets: acquisition staging grant, acquisition allocation
response and lifecycle allocation response. Every previous case/value/verdict
and expected schema row remains an exact logical prefix. There are 21 additional
positive and 27 negative expectations. URI syntax-positive userinfo and fragment
controls retain the acquisition owner's runtime prohibition and origin policy.

The obsolete profile's full attempt was deliberately closed after the defect
was confirmed. It retained 90 actual command records, including one SIGTERM exit
-15 instead of required 1; three commands remain unrecorded. That is a failed,
obsolete-profile attempt. All 49 owner inputs and 132 worker-copy files matched the
original hashes after closure. No interrupted result was counted as a negative
oracle or relabeled as the repaired profile.

Dependency preparation also retained an initial routing error: pre-router
environment overrides selected a different external output environment. Its
successful commands did not prepare the intended environment. The first
observation of the intended environment failed because packages were absent.
A separately recorded correction applied the overrides after the router and
prepared the intended isolated environment. The unexpected external environment
was preserved because its prior state was not inventoried; no speculative
restoration or host-default change was claimed.

The corrected effective CLI registry contains date, date-time, URI,
URI-reference and regex, including check-jsonschema's date-time override and
default ECMAScript regex implementation. Exact package/interpreter/callback
identities and 493 environment file/symlink records were frozen and checked.
The native macOS ARM64 tool profile uses CPython 3.14.7, uv 0.12.10,
check-jsonschema 0.38.0, jsonschema 4.26.0 and fifteen locked distributions.
Offline flags and disabled caches do not establish a network sandbox.

Four interpreter advisories remain recorded from the PSF's exact retained
primary records at revision `e903d0b393696d7f14f52fe9200d68c6fde0a715`:

| Advisory | Named affected path |
| --- | --- |
| [PSF-2026-36 / CVE-2026-15806](https://raw.githubusercontent.com/psf/advisory-database/e903d0b393696d7f14f52fe9200d68c6fde0a715/advisories/python/PSF-2026-36.json) | Stored credential matching in `urllib.request.HTTPPasswordMgr`. |
| [PSF-2026-37 / CVE-2026-17084](https://raw.githubusercontent.com/psf/advisory-database/e903d0b393696d7f14f52fe9200d68c6fde0a715/advisories/python/PSF-2026-37.json) | `stringprep` and the standard-library IDNA-2003 codec. |
| [PSF-2026-38 / CVE-2026-19672](https://raw.githubusercontent.com/psf/advisory-database/e903d0b393696d7f14f52fe9200d68c6fde0a715/advisories/python/PSF-2026-38.json) | POSIX tar extraction filters creating intermediate directories outside the destination. |
| [PSF-2026-39 / CVE-2026-15310](https://raw.githubusercontent.com/psf/advisory-database/e903d0b393696d7f14f52fe9200d68c6fde0a715/advisories/python/PSF-2026-39.json) | ZIP bzip2/LZMA/Zstandard decompression allocation. |

Independent preparation review found that the inspected fixed local JSON and
reference path, plus the added `re`-only URI parser, do not select those named
entry points. The exact inspected dependency wheel has seven DEFLATE members,
8,087 expanded bytes; selected member reads were bounded and did not extract
arbitrary paths. This is a path-applicability inference, not an exploit test,
whole-process syscall proof or interpreter-wide security clearance. No fixed
interpreter release or Homebrew advisory backport was established, and findings
about a different managed interpreter were not transferred to this interpreter.
The empty OSV response observed for `rfc3986-validator==0.1.1` at 15:42:01 UTC
applies only to that package/version and observation time. Arbitrary remote
references, archives, credential handling, hostname normalization and future
product/media profiles require their own applicable qualification.

All 48 complete values were run directly through their actual schema definitions
from 16:26:50 through 16:27:22 UTC: 21 accepted and 27 rejected with only URI-format
errors at the exact URL field. Three independent negative values, one per target,
then accepted with only URI checking disabled. Those three results are diagnostic
controls, not conformance. All 51 commands completed without timeouts, missing
results or unexpected exits. Source/mirror/prepared-value/environment/launcher
hashes stayed unchanged; all 102 streams and 257 output records were rehashed.
Independent result review checked every receipt, exact literal/oracle binding,
actual argument vector, error field/message and current source/environment
identity and passed. The nine rejecting lifecycle commands each report the same
URI error through two schema branches: 36 error objects across 27 rejecting
commands. No unrelated validation or infrastructure error was accepted.
The direct report SHA-256 is
`66ed381e4227cc9a5cc675df0ef9218c04405d92d242a9da254102aed05d0711`;
its result freeze is
`3cbb832a9b0ef4b7beb977dccb295201c5fc3c9118ab354ad03cb40dc2d31b87`.

## Actual full parsed-contract qualification

The repaired-profile run completed from 16:28:15.889580 through 19:52:08.577020
UTC on 2026-09-08. All 93 actual commands produced their expected results:
21 exited zero and 72 exited one, with no timeout, launch error, missing result
or captured-stream acceptance failure. Sixty nonzero commands exercised schema
or oracle rejection; twelve exercised deliberately broken references or removed
consumed libraries and retained their exact infrastructure diagnostics. Those
infrastructure failures are separate protocol controls, not invalid-value passes.

The direct lifecycle check took 1,662.880 seconds and its relocated repetition
1,879.313 seconds. The final restored lifecycle check exited zero in 1,771.190
seconds, followed by the restored signing-vector structure check in 1.096 seconds.
Durations use the recorded monotonic clock; UTC endpoints are retained separately.
All 186 streams were rehashed. All 33 machine inputs, 16 guidance inputs,
297 copied inputs across nine mirrors, and 493 environment entries matched their
frozen identities. The effective format profile also remained byte-identical.

The final report SHA-256 is
`9e341d78995f61f4acf1b6d868dfc5396dcd316b4cc3ec8fa683c009d64a0b40`;
the complete command ledger is
`66d838fa6baaa892155672375fc806557de64cf24ce812f1ad2a1514ba75d58f`.
This run binds only the repaired input freeze
`4ccbd0cb58693ea2131cdd5bc0e42b5ebaf6864b0cc76e2333032cffe04cb3a6`.
No historical outcome fills a missing command in this result.

The new static inventory contains 23 schemas, 13,645 local references and 11,827
cases: 1,963 positive and 9,864 negative. Acquisition has 249 cases, 94 positive
and 155 negative; lifecycle has 7,422 cases, 1,118 positive and 6,304 negative.
Lifecycle's 6,853 public cases comprise 1,047 positive and 5,806 negative across
166 targets; its 569 internal cases comprise 71 positive and 498 negative across
30 targets. Static counts are not validation results.

The complete protocol is eleven documented checks, eight relocated checks,
eighteen oracle mutations, four consumed broken references, eight consumed
missing libraries, all 42 off-diagonal pairings and two restored positive guards.
Eight independent mirrors each perform nine negative probes sequentially.
The retained validator budget is 5,700 seconds per command, ordinary budget 900;
input cap 32 MiB and captured stream acceptance 64 MiB after capture. No live disk
cap, peak-memory bound or product capacity follows. Infrastructure failures
must retain their exact diagnostic/sentinel and cannot pass as invalid values.

There are 33 frozen machine inputs and 16 frozen guidance files. Exact current
input identities for the final public evidence table are:

| Input | SHA-256 |
| --- | --- |
| Public lifecycle schema | `21a06ac3b04ef194eb75f6c3367cec20bdbdc370638704ea1b7416506e7c11b6` |
| Internal continuity schema | `761ac02635efecd7b27e7bbd1b2571908c738ca437b7801a9a134fd9cf01069e` |
| Exact-family entry | `b9aca47242cca3a22537e8aca8eb98cd505a6942ee2a9bb0875ef6435d6d65cb` |
| Lifecycle cases | `bada3df24ca8670c460162195604e2f7a80eb6518f2d9597e9d941ab5b2ecd71` |
| Lifecycle expectations | `f70bd15cbb8903a36d97b68f77c30c89bfc1becf7e4c5f5ee48d46c3dc3e3155` |
| Shared common schema | `61e94a0e646b2e992e662c7f5b3c30a17551936bfee3f87c7f3ae773302ff3b1` |
| Common cases | `77b51d313fcb8bbd3bd1071f14ccf406e2eace493c27263aaae18a97338b327e` |
| Common expectations | `f0191a31623cf21f758d0ea07464ac05a57eb2804df58dea8e2839d0cac91339` |
| Acquisition cases | `33e7ad3fd4c1885a244f7b03dd5a817656f7a9c07627661ef1188b80bf01fae0` |
| Acquisition expectations | `4053609323758cfb86424cc7d112a9c19625b34bb098d7794757fa2d94447040` |
| Validation project | `bf161267b9bdad0d26698eb42f34b4c39650f41098e8de10e1a3ed2f1928553a` |
| Validation lock | `787eb9c73efa517a751ae0c61cf4ee1bd1acf16eec12ac68c676493573ad5342` |
| Validation guide | `20a173d92478c6be894922e9d400ffc11d8ad20ec4065e288e1e49a89278d2ec` |
| Lifecycle owner | `20d5777269636d1586892a4ded5f53f486cd513250e7326999625acc46cbd9b3` |

## Review and remaining qualification

Three independent design reviewers passed the complete design before source
implementation. Separate schema, fixture reconciliation, common-rule and
documentation reviews passed after the recorded corrections. The revised
observer and combined freeze passed independent review before the fresh URI-profile
qualification began. Independent final result review confirmed all 93 command
records, exact argument and diagnostic classes, reconstructed mutations, complete
reference and case counts, unchanged source/mirror/environment identities and
all 186 raw stream hashes. The source and two proposed commit messages passed
separate disclosure and provenance review.

Parsed values do not qualify raw JSON admission, HTTP routing, authorization,
canonical hashes and cross-record equations, clocks and transaction fences,
worker/provider outcomes, storage deletion, propagation coverage, recognition,
retirement, independent continuity or restoration. Financial complete-delivery
throughput and later repair producers remain separate required implementations.
Existing signing-vector structure checks do not repeat or extend earlier
primitive signature qualification. A complete operational capability is not
advertised by publishing these schemas.

This engine checkpoint changes no maintained first-party Rust or Python source.
There is no numerical engine-source coverage claim. The testing and coverage
contract continues to require deterministic independently reviewed fixtures,
complete execution and strict coverage for each applicable source, package,
repository and profile when runtime and maintained support are introduced.
