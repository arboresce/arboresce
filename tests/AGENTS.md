# Shared verification

Read [the repository guidance](../AGENTS.md) first.

Own synthetic fixtures and tests spanning components. Keep local unit tests with their packages. Review expected meanings independently from implementation. Separate deterministic checks from optional external-service evaluations.
