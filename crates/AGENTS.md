# Rust packages

Read [the repository guidance](../AGENTS.md) first.

Own Rust libraries and executable packages. Keep package names explicit and the crate tree flat. Pure domain rules do not depend on transport or storage. Create a package only for a real dependency, testing, or release boundary.
